# Copy to scripts/.env.deploy and fill in Hostinger FTP credentials (gitignored).
FTP_HOST=ftp.czechsubaruclub.cz
FTP_USER=
FTP_PASS=
# Remote web root on Hostinger (check via hPanel → File Manager):
FTP_DIR=/public_html
